info:
  name: Kiosk 30 ohne Versionsnummer ändern
  type: http
  seq: 3

http:
  method: PUT
  url: "{{restUrl}}/:id"
  headers:
    - name: ""
      value: ""
      disabled: true
  params:
    - name: id
      value: "30"
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Kioskput",
          "email": "update@kiosk.de",
          "ist_geoeffnet": false,
          "homepage": "https://www.kiosk-neu.de"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "428"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
