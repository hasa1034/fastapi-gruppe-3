iinfo:
  name: Kiosk 1000 ändern
  type: http
  seq: 1

http:
  method: PUT
  url: "{{restUrl}}/:id"
  headers:
    - name: If-Match
      value: '"0"'
  params:
    - name: id
      value: "1000"
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Kioskput",
          "email": "update@kiosk.de",
          "ist_geoeffnet": false,
          "homepage": "https://www.kiosk-neu.de"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
    - expression: res.body
      operator: isString
    - expression: res.body
      operator: isEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
